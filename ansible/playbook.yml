---
  - name: DEPLOY NGINX
    hosts: frontend
    become: true
    vars_files:
    - group_vars/frontend.yml
    roles:
    - frontend

  - name: DEPLOY FAST API APPLICATION
    hosts: backend
    become: true
    vars_files:
    - group_vars/backend.yml
    roles:
    - backend
    